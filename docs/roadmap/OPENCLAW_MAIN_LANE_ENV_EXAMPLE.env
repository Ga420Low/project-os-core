# Optional break-glass only. Do not use as the daily operator auth path for the
# private Tailscale dashboard when gateway.auth.mode=trusted-proxy.
# OPENCLAW_GATEWAY_TOKEN=<set-on-server>
OPENCLAW_TZ=Europe/Paris
